STM32CubeIDE for VSCode is a great environment but if you are on Linux, then you find out that ITM debug printfs are not 
available.  This is an example project showing how to set up an alternative that still uses the USB
connection to your protoboard.
